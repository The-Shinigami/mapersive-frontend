import { ValueToIconPipe } from './value-to-icon.pipe';

describe('ValueToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
